# ===== 기본 =====
DATABASE_URL="file:./dev.db"
JWT_SECRET="change-me-in-prod"
PORT=4000

# ===== 실연동 (값이 있으면 실호출, 없으면 stub 폴백) =====
# AI 수업 요약 (Anthropic Claude). 키만 넣으면 즉시 실호출.
# ANTHROPIC_API_KEY="sk-ant-..."
# LLM_MODEL="claude-3-5-haiku-latest"

# 카카오 알림톡 (대행사/비즈메시지 — 계약 후 키/발신프로필/엔드포인트 입력)
# KAKAO_API_KEY=""
# KAKAO_SENDER=""        # 발신 프로필/번호
# KAKAO_API_URL=""       # 대행사 발송 엔드포인트

# 결제 PG (Toss/이니시스 등 — 클라이언트 SDK + 서버 승인 키)
# PG_PROVIDER="toss"
# PG_SECRET_KEY=""
